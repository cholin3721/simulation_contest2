# 발표 자료(PPT) 수정 체크리스트

코드 기준: `To_Be_Model/To_Be_Model.alpx` (커밋 `c971aa9` 이후)  
주문 DB: `To_Be_Model/database/db.script` · **8,858박스 · 60지점 · S/A 실측 91.7%**

> **KPI 숫자(256→640, 23%→44% 등)** 는 안정화 패치 이후 **재시뮬 1회** 돌리고 Charts 탭에서 갱신하세요. 아래 표의 「재측정」 항목만 바꾸면 됩니다.

---

## 슬라이드 1 · 표지
| 현재 | 바꿀 내용 | 이유 |
|------|----------|------|
| (변경 없음) | — | OK |

---

## 슬라이드 2 · 문제 배경
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `256 / 8,858 박스 (8 팔레트)` | **【재측정】** AS-IS 5시간 완료 박스/팔레트 수 |
| `1.6 → 4.0 박스/hr` | **【재측정】** TO-BE 시간당 처리량 |
| `+150 % 처리량` | **【재측정】** 증가율 (새 AS-IS·TO-BE 기준) |

**추가 한 줄 (각주):**  
「S/A 이론 비율 75% · **실제 주문 DB 91.7%**(8,120/8,858)」

---

## 슬라이드 3 · 시스템 개요
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `S/A 급 포함 박스 전체의 75%` | `S/A 급 포함: **이론 75%** (P(S∪A)) · **실제 주문 DB 91.7%**` |
| `B/C 급 전용 박스 전체의 25%` | `B/C 전용: **이론 25%** · **실제 DB 8.3%**` |
| `제함기 3대 · 30 box/min` | `제함기 3대 · **capacity 4** · 2초/box (병렬 4)` *(TO-BE·안정화 기준)* |

---

## 슬라이드 4 · AS-IS 병목
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `S 또는 A가 포함된 모든 박스(약 75%)` | `S/A 포함 박스: **이론 75% · 실제 DB 91.7%**` |
| `제함기2는 ... 25% 조건에서만` | 유지 (라우팅 로직 설명은 맞음) |
| 제함기 부하 % | **【재측정】** AS-IS 시뮬 Charts에서 제함기 가동률 캡처 |

---

## 슬라이드 5 · 모델링 가정
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `P(S∪A) = 0.75 도출` | `이론 P(S∪A)=0.75 · **실험 입력 DB는 91.7% S/A**` |
| `simulation_orders.xlsx` 언급 시 | `**내장 DB** (database/db.script · SHEET1 8,858건)` |
| 시뮬 시간 | `18,000초 (5시간) · **Stop at specified time**` *(코드 반영 완료)* |

---

## 슬라이드 6 · 제함기 부하 분산
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `S/A 박스 6,644개(75%)` | `S/A 박스 **8,120개 (91.7%, DB 실측)**` |
| `≈ 37.5% · 37.5% 균등` | **【재측정】** TO-BE 제함기1/2 부하 % |
| `selectOutput1 조건 1줄` | 유지 ✓ `delay_Erector1.size() <= delay_Erector2.size()` |

---

## 슬라이드 7 · AGV 최적화
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `−transporter.distanceTo(agent)` | **`−unit.distanceTo(agent)`** *(AnyLogic MoveByTransporter API)* |
| `총 3개 이동 블록에 적용` | `**5개** MoveByTransporter 블록 (S/A 피킹·투입·B/C 구간 포함)` |
| `약 23%` / `약 44%` | **【재측정】** `transporterFleet` 가동률 |
| (없음) | **추가 박스 1개:** 안정화 — `recognizeAllTransporters=true`, 교차로 경로 용량 제한, `delayToResumeMovement=1초` |

**블록 이름 (코드):** `moveByTransporter1`(A), `moveByTransporter2`(S), `moveByTransporter`(투입구), `moveByTransporter3/4`(B/C)

---

## 슬라이드 8 · 슬롯 동적 배정
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `AS-IS 문제점 · 정적 슬롯 배정` | `AS-IS: **기본 슬롯 로직(selectOutput6)** · TO-BE: **branchRemainingTotal + back-pressure 강화**` |
| `slotBranches[4] ← argmin(...)` | `**selectOutput6/7** · TYPE_EXIT_NUMBER · exitNumber에서 slotBranches 갱신` |
| (없음) | `selectOutput5`는 **B존 25% 확률 분기만** (팔레타이저 로직 아님) |

---

## 슬라이드 9 · 정량 결과
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `256 → 640` · `8→20 팔레트` · `1/60→3/60` | **전부 【재측정】** (안정화 패치 후 동일 18,000초) |
| `2.9%` / `7.2%` | **【재측정】** 완료율 |
| (없음) | 각주: 「수치는 AnyLogic Charts · 5시간 실험 기준」 |

---

## 슬라이드 10 · 민감도
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `20대 교착` / `35대 교착` | **【재측정】** `agvCount` UI 파라미터로 15·20·25·35·40 실험 |
| `25대 ★ 최적` | 유지 (방향성) · 수치만 재확인 |

---

## 슬라이드 11 · 결론
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `제함기1 100% → 94%` 등 | **【재측정】** |
| `완료 팔레트 8 → 20` 등 | **【재측정】** |
| `S/A 박스 75%` | `S/A **91.7% (DB)** · 이론 75%` |

---

## 슬라이드 12 · 임팩트 종합
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| 모든 %·박스·팔레트 수 | **【재측정】** (슬라이드 9와 동일 값으로 통일) |
| `코드 변경 3곳` | 유지 ✓ |

---

## 슬라이드 13 · 구현 요약
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| `−transporter.distanceTo(agent)` | **`−unit.distanceTo(agent)`** |
| `TransportTo · 3개` | **`MoveByTransporter` 5개** (핵심 3 + B/C 2) |
| `파렛타이저 슬롯 배정` | **`selectOutput6` (+ `selectOutput7`)** |
| `simulation_orders.xlsx` | **`To_Be_Model/database/db.script` (SHEET1)** |
| `TO_BE_Model.alpx` | **`To_Be_Model/To_Be_Model.alpx`** |
| `AGV 23% → 44%` 등 | **【재측정】** |
| (없음) | **4번째 구현 항목 추가:** AGV 교착 방지 — `recognizeAllTransporters`, `path29/67/68/21` 용량 제한 |

---

## 슬라이드 14 · Q&A
| 현재 문구 | 이렇게 바꾸기 |
|----------|-------------|
| 하단 KPI 4개 | 슬라이드 9 **【재측정】** 값과 **동일하게** 맞추기 |

---

## 재시뮬 후 숫자 넣는 곳 (한 번에)

1. AnyLogic → `To_Be_Model.alpx` · `agvCount=25` · Run 18,000초  
2. AS-IS도 동일 조건으로 1회  
3. Charts에서 복사:
   - 완료 박스 / 팔레트 수
   - 시간당 처리량
   - 완료 지점 수 (N/60)
   - AGV 가동률 (`transporterFleet`)
   - 제함기1/2/3 가동률
4. 슬라이드 **2, 4, 6, 7, 9, 10, 11, 12, 13, 14** 의 【재측정】 칸에 동일 값 붙여넣기

---

## README / 제출물 (코드 반영 완료)

- 경로: `To_Be_Model/` (대소문자 주의)
- 주문 데이터: xlsx 없음 → **내장 HSQLDB**
- 클론: `git clone` → `To_Be_Model/To_Be_Model.alpx` 실행
